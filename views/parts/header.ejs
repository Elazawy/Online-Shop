<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> </title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <style>
        .btn-custom { width: 100px; margin: 5px; }
        .form-control { width: 200px; display: inline-block; margin: 5px; }
        .input-group { margin: 10px 0; }
        .save-btn { width: 100px; background-color: #28a745; border-color: #28a745; }
        .save-btn:hover { background-color: #218838; border-color: #1e7e34; }
        .table-container { margin-top: 20px; }
        th, td { text-align: center; }
        .status-select { width: 100px; }
    </style>
</head>

<body>